# ══════════════════════════════════════════════════════════════════════════════
# Suite Great Expectations — silver.fbref_shooting
# Rôle : SIGNAL & DISTRIBUTIONS — première couche testable après scraping
# Contrainte spatiotemporelle : aucune (FBref couvre toutes les comp_category).
# Test dérive volumétrique : exclut la saison 2025-2026 (scraping en cours).
#
# NOTE : cette suite documente A-011 (standard_gls > standard_sot, 1 475 lignes)
# à sa SOURCE. Justifie un futur audits.scraping_targets_int_fbref_shooting.
# ══════════════════════════════════════════════════════════════════════════════

suite_name: silver.fbref_shooting
description: >
  Signal statistique de la source silver.fbref_shooting (données brutes normalisées
  par 02_process.py, avant rematching sur match_id + team_id). Couvre toutes les
  compétitions — pas de filtre saisonnier.

source:
  duckdb_path: db/football.duckdb
  query: |
    SELECT
      date, team, opponent, league_source, season,
      venue, result, result_1n2, comp_category, source,
      gf, ga,
      standard_gls, standard_sh, standard_sot, standard_sot_pct,
      standard_g_sh, standard_g_sot,
      standard_pk, standard_pkatt
    FROM silver.fbref_shooting

expectations:

  # ────────────────────────────────────────────────────────────────────────────
  # 1. VOLUMÉTRIE
  # ────────────────────────────────────────────────────────────────────────────
  - type: expect_table_row_count_to_be_between
    kwargs: { min_value: 40000, max_value: 100000 }
    severity: error
    meta: { description: "Empirique = 53 163 lignes." }

  # ────────────────────────────────────────────────────────────────────────────
  # 2. COMPLÉTION — NULL rate
  # ────────────────────────────────────────────────────────────────────────────
  - type: expect_column_values_to_not_be_null
    for_each_column:
      - date
      - team
      - opponent
      - league_source
      - season
      - venue
      - comp_category
      - source
      - gf
      - ga
      - standard_gls
      - standard_sh
      - standard_sot
      - standard_pk
      - standard_pkatt
    kwargs: { mostly: 1.0 }
    severity: error

  # standard_sot_pct : NULL quand standard_sh=0 (~3.6 %) → warn
  - type: expect_column_values_to_not_be_null
    kwargs: { column: standard_sot_pct, mostly: 0.90 }
    severity: warn
    meta: { description: "Empirique = 3.61 % NULL (standard_sh=0). Marge +5 pts." }

  - type: expect_column_values_to_not_be_null
    kwargs: { column: standard_g_sh, mostly: 0.90 }
    severity: warn
    meta: { description: "Empirique = 3.60 % NULL. Marge +5 pts." }

  - type: expect_column_values_to_not_be_null
    kwargs: { column: standard_g_sot, mostly: 0.88 }
    severity: warn
    meta: { description: "Empirique = 5.80 % NULL. Marge +5 pts." }

  # ────────────────────────────────────────────────────────────────────────────
  # 3. BORNES DE VALEURS
  # ────────────────────────────────────────────────────────────────────────────
  - type: expect_column_values_to_be_between
    kwargs: { column: standard_sot_pct, min_value: 0, max_value: 100, mostly: 1.0 }
    severity: error

  - type: expect_column_values_to_be_between
    kwargs: { column: standard_g_sh, min_value: 0, max_value: 1, mostly: 1.0 }
    severity: error

  - type: expect_column_values_to_be_between
    kwargs: { column: standard_g_sot, min_value: 0, max_value: 2, mostly: 1.0 }
    severity: error

  - type: expect_column_values_to_be_between
    for_each_column: [gf, ga, standard_gls]
    kwargs: { min_value: 0, max_value: 20, mostly: 1.0 }
    severity: error

  - type: expect_column_values_to_be_between
    kwargs: { column: standard_sh, min_value: 0, max_value: 60, mostly: 1.0 }
    severity: error

  - type: expect_column_values_to_be_between
    kwargs: { column: standard_sot, min_value: 0, max_value: 35, mostly: 1.0 }
    severity: error

  - type: expect_column_values_to_be_between
    for_each_column: [standard_pk, standard_pkatt]
    kwargs: { min_value: 0, max_value: 5, mostly: 1.0 }
    severity: error

  # ────────────────────────────────────────────────────────────────────────────
  # 4. BORNES STATISTIQUES
  # ────────────────────────────────────────────────────────────────────────────
  - type: expect_column_mean_to_be_between
    kwargs: { column: standard_sh, min_value: 9.0, max_value: 16.0 }
    severity: error
    meta: { description: "Empirique = 12.40" }

  - type: expect_column_mean_to_be_between
    kwargs: { column: standard_sot, min_value: 3.0, max_value: 6.0 }
    severity: error
    meta: { description: "Empirique = 4.31" }

  - type: expect_column_mean_to_be_between
    kwargs: { column: standard_sot_pct, min_value: 25.0, max_value: 45.0 }
    severity: error
    meta: { description: "Empirique = 35.24" }

  - type: expect_column_mean_to_be_between
    for_each_column: [gf, ga, standard_gls]
    kwargs: { min_value: 1.0, max_value: 2.0 }
    severity: error

  - type: expect_column_mean_to_be_between
    kwargs: { column: standard_g_sh, min_value: 0.07, max_value: 0.15 }
    severity: error

  - type: expect_column_mean_to_be_between
    kwargs: { column: standard_g_sot, min_value: 0.20, max_value: 0.40 }
    severity: error

  - type: expect_column_mean_to_be_between
    kwargs: { column: standard_pkatt, min_value: 0.05, max_value: 0.30 }
    severity: warn

  # ────────────────────────────────────────────────────────────────────────────
  # 5. COHÉRENCE CROISÉE — contraintes physiques métier
  # Doublon volontaire avec dbt (charte v2).
  # ────────────────────────────────────────────────────────────────────────────
  # Contraintes physiques dures : 0 violation empirique
  - type: expect_row_condition_to_hold
    kwargs:
      condition: "standard_sot <= standard_sh"
    severity: error

  - type: expect_row_condition_to_hold
    kwargs:
      condition: "standard_pk <= standard_pkatt"
    severity: error

  # A-011 confirmée côté silver : 1 475 violations
  - type: expect_row_condition_to_hold
    kwargs:
      condition: "standard_gls <= standard_sot"
    severity: warn
    meta:
      description: "A-011 : 1 475 lignes en violation. Bug scraping FBref confirmé côté silver — justifie un futur scraping_targets_int_fbref_shooting."
      fires_on_current_bug: true

  # A-011 : 5 lignes standard_gls > gf
  - type: expect_row_condition_to_hold
    kwargs:
      condition: "standard_gls <= gf"
    severity: warn
    meta:
      description: "A-011 : 5 lignes en violation. Anomalie mineure liée au même bug."
      fires_on_current_bug: true

  # ────────────────────────────────────────────────────────────────────────────
  # 6. DÉRIVE VOLUMÉTRIQUE INTER-SAISONS
  # ────────────────────────────────────────────────────────────────────────────
  - type: expect_partition_volume_stability
    kwargs:
      partition_col: season
      max_deviation_warn: 0.20
      max_deviation_error: 0.40
      min_seasons_history: 2
      exclude_partitions: ['2025-2026']
    severity: warn
